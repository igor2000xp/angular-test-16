import { FistCustomPipe } from './fist-custom.pipe';

describe('FistCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new FistCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
